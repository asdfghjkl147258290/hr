# HR Interview Guide

This is a simple Node.js + Express + Socket.IO app for HR interview photo guidance.

- Admin: /admin.html
- Candidate: /candidate.html

Run:

1. copy `.env.example` to `.env` and set ADMIN_PASSWORD
2. npm install
3. npm start

The app stores uploaded images in `public/uploads` and metadata in `data/`.
